apiVersion: v1
kind: Service
metadata:
  name: {{ include "email.fullname" . }}
  labels:
    app: {{ include "email.name" . }}
    release: {{ .Release.Name }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3002
      protocol: TCP
      name: http
  selector:
    app: {{ include "email.name" . }}
    release: {{ .Release.Name }}
