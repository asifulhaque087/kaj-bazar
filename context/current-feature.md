# Current Feature

## Status

Not Started

## Goals

<!-- Run /feature load to populate goals -->

## Notes

<!-- Additional context, constraints, or spec details go here -->

## History

- **Prisma + Neon PostgreSQL Setup** — Installed Prisma 7, created full schema (Doctor, Patient, Prescription, Plan, Payment), configured prisma.config.ts with dotenv, added PrismaPg adapter singleton in src/lib/db.ts, applied initial migration to Neon dev branch.
- **Auth Setup (NextAuth v5)** — Installed next-auth@beta, bcryptjs, jose. Added provider field to Doctor schema. Split auth config (auth.config.ts for edge, auth.ts for full). Google OAuth upserts Doctor on first sign-in; Credentials provider uses bcrypt. Internal JWT access/refresh tokens signed via jose. Route protection via src/proxy.ts. Dashboard placeholder at /dashboard.
- **Homepage — Marketing Landing Page** — Recreated the `prototypes/homepage.html` mockup as a real App Router page. Added ShadCN scaffolding (components.json, lib/utils cn, Button primitive w/ variants), swapped to DM Sans / DM Serif Display fonts, and added mockup design tokens + decorative CSS under `@theme` in globals.css. Built `src/components/landing/`: server sections (Hero w/ static A4 Rx preview, StatStrip, Problem, Features, Builder, Workflow, Pricing, FinalCta, Footer) driven by a shared `data.ts`, plus minimal client components (SiteHeader scroll-shadow + drawer, BuilderTabs switch + auto-cycle, Reveal IntersectionObserver). All primary CTAs route through one `SIGN_IN_HREF` constant. Also regenerated the stale Prisma client (provider field) that was blocking the build.
- **Dashboard — Doctor Workspace** — Recreated `prototypes/dashboard.html` as six real App Router routes (`/dashboard`, `patients`, `prescriptions/new`, `prescriptions/history`, `reports`, `settings`) sharing one server `layout.tsx` (Sidebar + TopBar). Added deps `react-hook-form`, `zod`, `@hookform/resolvers`, `jspdf`. Hand-rolled ShadCN-style primitives in `src/components/ui/` (input, textarea, select, checkbox, badge, card, table, tabs) styled to the mockup — avoids a conflicting `bg-background` theme layer over the Tailwind v4 `@theme` tokens; scoped mockup CSS migrated into globals.css using tokens. Shell + islands in `src/components/dashboard/` with one `DashboardProvider` (drawer, toast, doctor profile, in-memory Rx history + builder prefill). `PrescriptionBuilder` uses RHF + zod + `useFieldArray`; reusable `Autocomplete`/`ChipSelect`/`TagInput`/`MedicineRow`; live A4 preview + print share one escaped-HTML renderer (`preview-html.ts`); PDF via dynamic jsPDF import. Sidebar active state via `usePathname()`; top-bar search routes to `/dashboard/patients?q=`; row-click/Repeat prefill the builder via shared store. Date strings computed server-side and passed as props to avoid hydration mismatch. UI + form behavior only — seed data in `data.ts`, no persistence. Unit tests for pure helpers + zod schemas (26 passing); build + lint clean. Browser interaction verification still pending (routes are auth-gated by `proxy.ts`).
- **Turborepo Setup** — Converted the single Next.js repo into a Turborepo monorepo (in place) managed with pnpm. Moved the entire app (`src`, `prisma`, `public`, all configs, `package.json`) into `apps/web` via `git mv` (100% renames, history preserved); renamed the app package to `web`. Added root workspace config: `pnpm-workspace.yaml` (`apps/*`, `packages/*` — ready for a future `apps/api` NestJS backend + shared `packages/`), root `package.json` with turbo-driven `dev/build/start/lint/test` scripts (`packageManager: pnpm@11.0.8`, `turbo ^2`), and `turbo.json` task pipeline (build outputs `.next/**`, persistent `dev`/`start`/`test:watch`). Switched lockfiles: removed `package-lock.json`, generated `pnpm-lock.yaml`; approved native build scripts (`@prisma/engines`, `prisma`, `sharp`, `unrs-resolver`) via `allowBuilds` in the workspace file. Updated `.gitignore` (depth-agnostic `node_modules`/`.next`, added `.turbo`, fixed prisma client path to `apps/web/src/generated/prisma`) and CLAUDE.md commands to pnpm/turbo + a monorepo note. `prototypes/` kept at root as design reference. Verified all green from root via turbo: `pnpm install`, `prisma generate`, `pnpm test` (26 passing), `pnpm lint`, `pnpm build` (9 routes, `.env` detected, no monorepo-root warnings), and `pnpm dev` (Ready, `/` → 200).
- **NestJS Backend API Setup** — Scaffolded `apps/api` as a second pnpm workspace (package `api`) alongside `apps/web` — a hand-written NestJS 11 app (no nested CLI/git scaffold): `package.json`, `nest-cli.json`, `tsconfig.json` (strict) + `tsconfig.build.json`, flat `eslint.config.mjs` (typescript-eslint recommended). `src/main.ts` bootstraps Nest and reads `PORT` (3001) + `CORS_ORIGIN` (http://localhost:3000) via `ConfigService`, enabling CORS for that origin; `src/app.module.ts` registers a global `ConfigModule.forRoot({ isGlobal: true })` + `HealthController`; `src/health/health.controller.ts` serves `GET /health` → `{ status: "ok" }`. Added `.env` / `.env.example` (`PORT`, `CORS_ORIGIN`). Root wiring: `turbo.json` build outputs add `dist/**`; `pnpm-workspace.yaml` `allowBuilds` adds `@nestjs/core` (opencollective postinstall); `.gitignore` adds `dist/` and un-ignores `.env.example` (`!.env.example` after `.env*`); existing dev/build/start/lint turbo tasks already glob all workspaces. Foundation only — no DB, auth, or business endpoints (deferred); shared `packages/` for Prisma client/types kept in mind but not built. Verified all green from root: `pnpm lint` (2/2), `pnpm build` (2/2), `pnpm test` (26 passing, web unaffected), and `pnpm dev` starts both (api 3001 → `{"status":"ok"}`, web 3000 → 200) with no port conflict and CORS header present.
- **Drizzle Setup (api) + strip Prisma/NextAuth from web** — Set up Drizzle ORM in `apps/api` and removed Prisma + NextAuth from the repo. Added `drizzle-orm` + `pg` (deps) and `drizzle-kit` + `tsx` + `dotenv` + `@types/pg` (dev) with `db:generate`/`db:migrate`/`db:studio` scripts. Authored schemas one-file-per-table under `src/schemas/` + barrel `index.ts`: `enums` (gender, prescription_status, food_timing, payment_method), `user` (renamed from Doctor; dropped `monthlyPrescriptions`; added `planId` FK→plans `set null` + `planExpiresAt`), `patient`, `prescription` (jsonb `medicines` `$type<MedicineEntry[]>`, text[] arrays), `plan`, `permission`, `plan-permission`, `role`, `role-permission` (fixed the spec's duplicate name; value-less by design), `user-role` (new M2M), `user-permission-quota` (purchased-credit model: integer `grantedValue` + `remaining`, no calendar reset), `payment` (text `userId` FK nullable `set null`, `planName` text, enum method); cross-table `relations()` for all 11 tables in a single `relations.ts` (avoids circular imports). Tooling: `drizzle.config.ts` (schema `src/schemas/index.ts`, out `drizzle/migrations`), `drizzle/drizzle.d.ts` (`DrizzleDB`), `drizzle/migrate.ts` (pg Pool + `tryit`); excluded `drizzle/` + `drizzle.config.ts` from the Nest build. Switched Nest to the **SWC builder** (`@swc/core`/`@swc/cli`, `builder=swc` + `typeCheck`) + `baseUrl`/`paths` so `@/` aliases resolve at runtime; app code (`main.ts`, `app.module.ts`, `DrizzleModule`) uses `@/`. Global `DrizzleModule` provides a `DRIZZLE` token (lazy `pg` Pool + `drizzle(pool, { schema })`) from `ConfigService.DATABASE_URL`; `DATABASE_URL` added to api `.env`/`.env.example`. Removed from `apps/web`: `auth.ts`, `auth.config.ts`, `proxy.ts`, `lib/db.ts`, `types/next-auth.d.ts`, `app/api/auth/*`, `src/generated/prisma`, `prisma/` (+migrations), `prisma.config.ts`, and deps `@prisma/client`/`@prisma/adapter-pg`/`prisma`/`next-auth`/`bcryptjs`/`jose`/`pg`/`@types/pg`/`@types/bcryptjs`/`dotenv` — web now has no auth/route-protection (api takes over later); trimmed web `.env`. Root cleanup: `.gitignore` (dropped prisma-client line), `pnpm-workspace.yaml` `allowBuilds` (removed `prisma`/`@prisma/engines`, added `@swc/core`/`esbuild`), CLAUDE.md (Prisma→Drizzle command + DB-in-api note). Generated + applied initial migration `0000_hard_stature.sql` (11 tables, FKs, enums) against Neon (authorized for the empty DB). Verified green from root: `pnpm lint` (2/2), `pnpm build` (2/2; api SWC + TSC 0 issues; web no longer emits the nextauth route/middleware), `pnpm test` (26), api boots with `DrizzleModule` initialized and `/health` → `{"status":"ok"}`. Deferred: entitlement enforcement (top-up/decrement guard, daily expiry→downgrade cron via `@nestjs/schedule`), shared `packages/`, and `project-overview.md` still documents the old Prisma/NextAuth stack.
- **Auth Setup — Passport.js (boilerplate)** — Set up JWT + Google OAuth authentication scaffolding in the NestJS API, plus the repo's first shared workspace package. Created `packages/common` (`@my-patient/common`): a buildable (tsc → `dist`) package exporting the Go-style `tryit` error-tuple util (`[data, error]`), consumed by `apps/api` via `workspace:*` (turbo `^build` builds it first; left `drizzle/migrate.ts`'s standalone `tryit` untouched to avoid coupling the ops script to the package build). Authored `apps/api/src/auth/`: `AuthService` (`generateTokens` signing access [JwtModule defaults] + refresh [overridden secret/expiry] and persisting `users.refreshToken`; `refreshAccessToken`; `getCookieSettings` httpOnly cookies via `ms()`; `isTokenExpired`; `validateSocialUser` upsert-on-email), `JwtStrategy` (`passport-jwt`, Bearer-from-header, `ACCESS_TOKEN_SECRET`), `GoogleStrategy` (`passport-google-oauth20`, reads `GOOGLE_CLIENT_ID`/`SECRET`/`CALLBACK_URL`), `AccessTokenGuard` (extends `AuthGuard('jwt')`; reads cookies, silently rotates an expired access token via the refresh token, re-sets cookies, forwards a Bearer header), `AuthController` (`GET /auth/google` + `/auth/google/callback` → sets cookies, redirects to `CLIENT_URL`), `GetUser` param decorator, class-validator DTOs (`ValidateSocialUserDto`, `RefreshAccessTokenDto`), and internal interfaces (`JwtPayload`/`AuthUser`/`AuthTokens`). **gRPC → HTTP adaptation:** `throwGrpcError(...)` → Nest `InternalServerErrorException`/`UnauthorizedException`; reference `AuthTable` → local `users` schema (DTO `username` → `name`; `profilePicture` carried but not persisted — no column). Wired `cookie-parser` + a global `ValidationPipe({ whitelist, transform })` in `main.ts` and enabled CORS `credentials: true`; registered `AuthModule` in `app.module.ts` (imports `PassportModule` + `JwtModule.registerAsync`). **express gotcha:** used `import type { Request, Response } from "express"` so `emitDecoratorMetadata` on `@Res()` doesn't emit a runtime `require('express')` (which pnpm's strict layout doesn't hoist into `apps/api`) — no separate `express` install needed. Cast token-expiry env strings to `ms.StringValue` to satisfy `@nestjs/jwt` `signOptions.expiresIn` typing. Deps added to api: `@nestjs/passport`, `@nestjs/jwt`, `passport`, `passport-jwt`, `passport-google-oauth20`, `cookie-parser`, `ms`, `class-validator`, `class-transformer` (+ `@types/*`). Env vars added to `.env`/`.env.example`: `ACCESS_TOKEN_SECRET`/`EXPIRATION`, `REFRESH_TOKEN_SECRET`/`EXPIRATION`, `GOOGLE_CLIENT_ID`/`SECRET`/`CALLBACK_URL` (reconciled spec's `AUTH_GOOGLE_*` naming), `CLIENT_URL`. Boilerplate only — no sign-in/sign-up/password-hashing endpoints (later feature). Verified green from root: `pnpm lint` (3/3), `pnpm build` (3/3; api TSC 0 issues, 28 files via SWC), `pnpm test` (26 passing); api boots with `AuthModule` initialized, `/health` → 200, `/auth/google` → 302 to `accounts.google.com` with correct `redirect_uri`/scope/`client_id` — all without `express` as a direct dep.
- **User Registration + RBAC/billing seed** — Seeded the RBAC + entitlement data and built email/password registration on the existing auth scaffolding. New `apps/api/src/auth/permissions.ts`: a `Permission` enum + `PERMISSION_CATALOG` (key/name/description/`numeric`|`boolean` type) — one source of truth that both seeds the `permissions` table and will back the future `@RequiredPermission` decorator (no key drift). New `rbac.constants.ts`: stable slugs (`free`/`pro`, `doctor`/`super-admin`) that registration resolves by. New idempotent seed `apps/api/drizzle/seed.ts` (its own `pg` Pool like `migrate.ts`, relative imports since tsx ignores `@/` path aliases, `onConflictDoNothing` throughout) run via a `db:seed` script (`tsx drizzle/seed.ts`): 4 permissions (`prescriptions:create`/`patients:create` numeric, `ai:use_features`/`reports:view` boolean), free (`$0`) + pro (`$9`) plans, 8 plan-permissions (free `15`/`50`/`false`/`true`; pro all `unlimited`/`true`), doctor + super-admin roles, 3 role-permissions (boolean capabilities only — doctor→`reports:view`, super-admin→`reports:view`+`ai:use_features`). Registration: `RegisterUserDto` (class-validator: name/email/password 8–72) → `AuthService.registerUser` (bcryptjs hash, salt 10; resolves free plan + doctor role by slug → `500` if unseeded; one `db.transaction` creates the user [`provider:"credentials"`, `planId`], inserts the `userRoles` row, and snapshots the free plan's `planPermissions` into `userPermissionQuotas` via pure exported helpers `parseQuotaValue` [`unlimited`→`-1` `UNLIMITED_QUOTA`, `true`→1, `false`→0, numeric as-is] + `buildQuotaRows` [`grantedValue===remaining`]; then `generateTokens`). Pre-check select rejects duplicate email with `409` (unique constraint is the backstop; concurrent dupes surface as `500`). `POST /auth/register` (`@HttpCode(201)`, `@Res()` like the Google callback) sets both httpOnly cookies and returns an allowlisted user body (no password/tokens). `tryit` used throughout — no raw try/catch. Stood up Vitest for `apps/api` (first api tests): `vitest.config.ts` with `unplugin-swc` (`swc.vite()`) so Nest legacy decorators + `emitDecoratorMetadata` work under Vitest (esbuild can't), `@` alias, `setupFiles:["reflect-metadata"]`; `test`/`test:watch` scripts. 10 new tests (`auth.service.test.ts` with a hand-rolled chainable Drizzle stub keyed by table identity + bcrypt mock: helpers, happy/duplicate/unseeded paths; `auth.controller.test.ts`: cookie-set + sanitized body). Deps added to api: `bcryptjs` (dep; ships own types — no `@types`); `vitest`, `unplugin-swc` (dev). Verified green from root: `pnpm lint` (3/3), `pnpm build` (3/3; api TSC 0 issues, 33 files via SWC), `pnpm test` (**36**: web 26 + api 10); seed applied to the Neon **dev** branch (idempotent on re-run); live `POST /auth/register` → `201` + two httpOnly cookies (5m/7d) + safe body, duplicate → `409`, short password → `400`, and DB confirmed `$2b$` hash, `plan_id=1`, `doctor` role, and the 4-row free-plan quota snapshot. Decisions (user-approved): bcrypt, populate quotas at registration, the core permission set, and a `tsx` `db:seed` runner. Deferred (unchanged): quota-decrement guard + plan-expiry→downgrade cron; optional `23505`→`409` hardening for concurrent duplicate emails; email case-normalization (OAuth path is also unnormalized); `project-overview.md` still documents the old stack in places.
- **User Login (email/password)** — Built `POST /auth/login` on the existing Passport/JWT auth scaffolding. New `LoginUserDto` (`email` + `password` 8–72, mirrors `RegisterUserDto` bounds). New `AuthService.loginUser`: select user by email → guard against not-found **and** null `password` (OAuth-only users can't password-login) → `bcrypt.compare` → reuse existing `generateTokens` (which also persists the refresh token); every async step wrapped in `tryit` (no raw try/catch). Both "unknown email" and "wrong password" throw the same generic `UnauthorizedException` (401) so the response never reveals which failed — no account enumeration. New `AuthController.login` (`@HttpCode(200)`, `@Res()` like register/Google callback): calls the service, sets both httpOnly cookies via `getCookieSettings`, returns the allowlisted user body (`id`/`name`/`email`/`isOnboarded`/`planId`/`createdAt` — no password/tokens). **Decision (user-approved): email-only login**; the spec's "or phone" was deferred because `users` has no unique phone column (only nullable, non-unique `contactNumber`, never collected at registration) — phone login waits on a future schema/registration change. Tests: 5 new cases — extended the bcrypt mock with `compare` and the hand-rolled Drizzle stub with a `foundUser` row; service covers happy path, unknown email → 401, OAuth/no-password → 401, wrong password → 401; controller covers cookie-set + sanitized body. Verified green from root: `pnpm lint` (3/3), `pnpm build` (3/3; api TSC 0 issues, 34 files via SWC), `pnpm test` (**41**: web 26 + api 15, up from 10); live against the Neon **dev** branch (registered a fresh user, then) correct login → `200` + two httpOnly cookies (access 5m / refresh 7d) + safe body, wrong password → `401`, unknown email → `401`, short password → `400` (ValidationPipe). No new deps. Deferred (unchanged): phone login (needs schema); a `/auth/logout` (clear cookies + null `refreshToken`); rate-limiting on login; email case-normalization.
- **Email Send & Forgot/Reset Password** — Configured transactional email in the NestJS API and built the forgot/reset-password flow. **Decision (user-approved): DB-column reset tokens** (vs. stateless JWT) — added `resetTokenHash` (text) + `resetTokenExpiresAt` (timestamp) to `users` and generated/applied migration `0001_misty_purple_man.sql` to the Neon **dev** branch. New `apps/api/src/mail/`: `MailModule` wraps `@nestjs-modules/mailer` (nodemailer + `EjsAdapter`) with SMTP transport + default `from` read from env, and points the EJS template dir at `join(__dirname, "templates")`; `MailService.sendPasswordResetEmail` renders + sends and **swallows/logs** send failures (returns a boolean) so the caller never reveals account existence; a polished, email-safe `templates/forgot-password.ejs` (inline styles, brand navy/teal, preheader, CTA + raw-link fallback). **EjsAdapter import gotcha:** must be `@nestjs-modules/mailer/adapters/ejs.adapter` (the package `exports` map blocks the deeper `/dist/adapters/...` path at runtime even though TSC/SWC resolve it). Templates are copied into `dist` via a nest-cli `assets` rule (`mail/templates/**/*`, `watchAssets`) so `__dirname/templates` resolves in the built app. Auth: exported pure `hashResetToken` (SHA-256 hex of the raw token — only the hash is stored); `AuthService.forgotPassword` (look up by email → **silent no-op** for unknown or OAuth-only/no-password users → `randomBytes(32)` raw token, store its hash + `RESET_TOKEN_EXPIRATION` (15m) expiry, email `${RESET_PASSWORD_URL}?token=<raw>`); `AuthService.resetPassword` (look up by token hash → reject missing/expired with `400 BadRequestException` → bcrypt-hash new password, clear `resetTokenHash`/`resetTokenExpiresAt` **and** `refreshToken` to invalidate sessions). All async via `tryit`. New `ForgotPasswordDto` (`@IsEmail`) + `ResetPasswordDto` (`token` non-empty, `password` 8–72). `AuthController`: `POST /auth/forgot-password` (`@HttpCode(200)`, always returns the same generic message — no enumeration) + `POST /auth/reset-password` (`@HttpCode(200)`, plain JSON return, no `@Res`/cookies); `MailModule` imported into `AuthModule`. Env added to `.env`/`.env.example`: `SMTP_HOST`/`PORT`/`SECURE`/`USER`/`PASS`, `MAIL_FROM`, `RESET_PASSWORD_URL`, `RESET_TOKEN_EXPIRATION` (dev uses an Ethereal test inbox). Deps added to api: `@nestjs-modules/mailer`, `nodemailer`, `ejs` (+ `@types/nodemailer` dev). Tests: **9 new** — service covers `hashResetToken` determinism, forgot happy path (hash+future-expiry stored, email carries raw-token link + 15m), unknown-email + OAuth-only silent no-ops, reset happy path (password set + all three columns cleared), unknown-token + expired-token → 400; controller covers both endpoints' delegation + exact response messages (mock DB `update` capture split into reset/password/refresh buckets; `makeService` gains a `MailService` mock + reset-related config keys). Verified green from root: `pnpm lint` (3/3), `pnpm build` (3/3; api TSC 0 issues, 38 files via SWC; `forgot-password.ejs` copied into `dist/mail/templates`), `pnpm test` (**50**: web 26 + api 24, up from 41). Live against Neon **dev** + Ethereal SMTP: register → `forgot-password` for known **and** unknown email returns the identical 200 message; email sent with no error; DB confirmed 64-char hash + future, non-expired expiry; `reset-password` with a (seeded-hash) valid token → 200 → login with new password → 200, old password → 401, **token reuse → 400** (single-use, columns nulled), bogus token → 400, short password → 400 (ValidationPipe). Deferred: rate-limiting on `forgot-password`; a frontend `/reset-password` page; capturing the nodemailer preview URL; `project-overview.md` still documents the old stack in places.
- **Permission Guard** — Added a reusable RBAC/entitlement guard to the NestJS API on the existing seeded permission data. New `apps/api/src/auth/decorators/require-permission.decorator.ts`: `@RequirePermission(permission: Permission)` → `SetMetadata(REQUIRE_PERMISSION_KEY, …)`; arg typed as the `Permission` enum (the single source of truth that also seeds the catalog) so a route can't gate on a non-existent key — reconciled the spec pseudo-code's mistaken `PermissionType` arg. New `apps/api/src/auth/guards/permissions.guard.ts`: `PermissionsGuard implements CanActivate`, injects the DB via the real `DRIZZLE` token (`@Inject(DRIZZLE)`, not the pseudo-code's `'DRIZZLE_CONNECTION'`) + `Reflector`. **Decision (user-approved): quota-first resolution.** Flow: no metadata → public `true`; missing `req.user` → `UnauthorizedException` (note: `req.user` is `AuthUser` with **`userId`**, not `id` as the spec assumed); then check `userPermissionQuotas` for `(userId, permissionKey)` — row with `remaining === UNLIMITED_QUOTA` (`-1`) or `> 0` → allow, `remaining === 0` → **plan/subscription block** → `ForbiddenException` whose message contains "Upgrade to Pro"; no quota row → fall back to a role check (`userRoles` ⋈ `rolePermissions` on `roleId`, filtered by `permissionKey`) — granted → allow, miss → plain `UnauthorizedException` (role failure). Reconciled the pseudo-code's string `maxValue`/`'false'`/`'0'` checks to the real integer `remaining` column. Both DB queries wrapped in `tryit` (errors → `InternalServerErrorException`); **read-time access only — no quota decrement** (the atomic decrement-on-use guard stays a separate deferred feature). Wired into `AuthModule` `providers` + `exports` so any module under `apps/api/src` can apply it. Tests: **11 new** — guard (9, hand-rolled chainable Drizzle stub keyed by table identity à la `auth.service.test.ts`, with optional thenable-reject error flags): public route, missing user → 401, quota remaining/unlimited → allow, exhausted → 403 + `/Upgrade to Pro/`, role-granted → allow, role-miss → 401, quota-query error → 500, role-query error → 500; decorator (2): metadata set on a method and on a class. Verified green from root: `pnpm lint` (3/3), `pnpm build` (3/3; api TSC 0 issues, 42 files via SWC), `pnpm test` (**61**: web 26 + api 35, up from 50). No new deps, no schema/migration. Not yet applied to any controller — pure reusable infrastructure (no live route exercises it until a controller adopts it), so verification is unit-test only. Discussed (no code change): the `permissions.type` column is write-only today (seeded, never read) but kept as the discriminator the future decrement guard will branch on (boolean = check-only, numeric = check-then-decrement); booleans-as-integers (`0`/`1`, `-1` = unlimited) confirmed as good practice given a `type` discriminator. Deferred: applying the guard to real endpoints; the quota-decrement guard + plan-expiry→downgrade cron; naming the `0` sentinel (e.g. `DENIED`) to match `UNLIMITED_QUOTA`.
- **Plan CRUD (subscription module)** — First business-resource module on the NestJS API, and the first controller to actually apply the auth guards. New `apps/api/src/subscription/` (`SubscriptionModule` imports `AuthModule` for the exported guards; DB via the global `DRIZZLE` token) registered in `app.module.ts`. `SubscriptionService` (`@Inject(DRIZZLE)`, all async via `tryit` — no raw try/catch): `createPlan` (derive slug from name via exported pure `slugify()` → `400` if the name has no alphanumerics; pre-check select → `409 ConflictException` on slug collision; one `db.transaction` inserts the `plans` row + its `planPermissions` rows together, per the spec Note that a plan and its permissions are created in the same call), `getPlans` (all plans, each merged in JS with its permissions — two `.select()`s, not the relational query API, to match the codebase + the hand-rolled test stub), `getPlan` (→ `404 NotFoundException`), `updatePlan` (existence check → `404`; transaction updates name/price and, when `permissions` is supplied, **replaces** the full grant set via delete-then-insert; **slug is intentionally immutable** so the `free`/`pro` slugs registration resolves can't drift), `deletePlan` (`.returning()` → `404` if no row; `plan_permissions` cascade via FK, `users.plan_id` set null). `SubscriptionController` `@Controller("plans")`: `GET /plans` + `GET /plans/:id` **public** (pricing page); `POST` (`201`) / `PATCH` / `DELETE` (`200`) guarded by `@UseGuards(AccessTokenGuard, PermissionsGuard)` + `@RequirePermission(...)`; `:id` via `ParseIntPipe`. DTOs: `CreatePlanDto` (name 2–100, `priceMonthly` `@IsInt`/`@Min(0)`, `permissions` `@ArrayMinSize(1)` of nested `PlanPermissionInput` = `{ permissionKey @IsEnum(Permission), value string }`) + `UpdatePlanDto` (all `@IsOptional`, reuses `PlanPermissionInput`). RBAC: added four **boolean** keys — `plans:create`/`read`/`update`/`delete` — to the `Permission` enum + `PERMISSION_CATALOG`, granted to the **`super-admin` role only** in `seed.ts`'s `ROLE_PERMISSION_SEED` (loosened `PLAN_PERMISSION_SEED` to `Partial<Record<Permission,string>>` since plans don't grant the admin caps). **Guard interaction:** these are role-capabilities (no quota row), so the quota-first `PermissionsGuard` resolves them via its role-fallback branch — a plain `doctor` hits the role miss → `401`. **Decisions (user-approved):** auto-derive slug (not a DTO field); four granular CRUD keys (vs. one `plans:manage`); super-admin only; reads left open. Tests: **14 new** Vitest (`subscription.service.test.ts`, same chainable-thenable Drizzle stub keyed by table identity + op): `slugify` (3), create happy/empty-slug-`400`/duplicate-`409`/tx-error-`500`, getPlans merge, getPlan happy/`404`, updatePlan happy/`404`, deletePlan happy/`404`. Verified green from root: `pnpm lint` (3/3), `pnpm build` (3/3; api TSC 0 issues, 48 files via SWC), `pnpm test` (**75**: web 26 + api 49, up from 61). Re-seeded the Neon **dev** branch (now 8 permissions, 7 role-permissions). Live against dev: open reads `200`; `POST` no-auth `401`; `POST` as doctor `401` (role lacks `plans:create`); promoted a user to super-admin then `POST` `201` (slug `team-plan` derived) → duplicate name `409` → `PATCH` `200` (price changed, slug unchanged, perms replaced) → `DELETE` `200` `{id}` → `GET` `404`; invalid `permissionKey` `400` (ValidationPipe `@IsEnum`). No new deps, no schema change/migration. Left a `super-admin` test user in the dev DB. Deferred (unchanged): the quota-decrement guard + plan-expiry→downgrade cron; `project-overview.md` still documents the old stack in places.
- **Subscription (purchase + payments list + plan-expiry cron)** — Second business module on the subscription/billing surface, built on the existing Plan CRUD + RBAC. New `apps/api/src/subscription/billing.service.ts` (`@Inject(DRIZZLE)`, all async via `tryit` — no raw try/catch) with three exported pure helpers: `addMonths`, `computeSubscriptionPeriod` (base = `planExpiresAt > now ? planExpiresAt : now`; `endDate = base + months` → the new `planExpiresAt`, so an active plan **stacks** and an expired/absent one **starts now**), and `mergeQuota` (raw `plan_permissions.value` carries the semantics — `"unlimited"` always wins → `-1`; `"true"/"false"` **overwrite** → `1/0`; a numeric string is **added** onto the existing `grantedValue`/`remaining`, and a numeric add onto an already-unlimited balance stays unlimited). `BillingService.subscribe(userId, dto)`: slugify `dto.plan` → resolve `plans.slug` (→`404`), read the user's current `planExpiresAt` (→`404` if user gone), load the plan's `planPermissions` + the user's existing quotas, then one `db.transaction` upserts each merged quota (`onConflictDoUpdate` on the `(userId, permissionKey)` PK), updates `users.planId`/`planExpiresAt`, and inserts a `payments` row (`durationMonths = 1` const, `amountPaid = plan.priceMonthly`, caller-supplied demo `transactionId`, default `sslcommerz` method, `start/endDate` from the period) — tx failure → `500`. `listPayments({page,limit})`: `count()` + `desc(createdAt)` `limit`/`offset` page, returns `{data,page,limit,total,totalPages}`. `downgradeExpiredUsers(now)`: resolve free plan + doctor role + free plan-permissions (→`500` if unseeded), find doctor-role users with `planExpiresAt IS NOT NULL AND < now` (free users have null expiry → never touched), and per-user in a tx reset `planId`→free / `planExpiresAt`→null and overwrite quotas (delete + `buildQuotaRows` insert); a single user's tx failure is logged and the sweep continues; returns the count. New `BillingController` `@Controller("billing")`: `POST /billing/subscribe` (`201`, `@UseGuards(AccessTokenGuard, PermissionsGuard)` + `@RequirePermission(SUBSCRIPTION_CREATE)`, `@GetUser()` → `user.userId`) and `GET /billing/payments` (`@RequirePermission(PAYMENTS_READ)`). New `PlanExpiryCron` (`@nestjs/schedule`, `@Cron(EVERY_DAY_AT_MIDNIGHT)`) — a thin `tryit` wrapper over `downgradeExpiredUsers` so the logic stays unit-testable without the scheduler; registered `ScheduleModule.forRoot()` in `app.module.ts`. DTOs: `SubscribeDto` (`plan` + `transactionId`, non-empty strings) + `ListPaymentsDto` (`@Type(()=>Number)` `page`≥1 / `limit` 1–100, defaults 1/20). RBAC: added two **boolean** keys — `subscription:create` (granted to the **doctor** role → any logged-in doctor can subscribe, resolved via the guard's role-fallback since doctors have no quota row for it) and `payments:read` (**super-admin** only) — to the `Permission` enum + `PERMISSION_CATALOG` + `ROLE_PERMISSION_SEED`. **Decisions (user-approved):** payments list = **all** payments, super-admin only (vs. per-user); `plan` resolved by slugified name (handles `"Pro"`/`"pro"`); monthly billing (`durationMonths = 1`); subscribe open to any doctor. Tests: **15 new** Vitest (`billing.service.test.ts`, same chainable-thenable Drizzle stub keyed by table identity + `joined`/`ordered` flags to disambiguate the expired-users join and the payments count-vs-list queries): `addMonths`, `computeSubscriptionPeriod` (now / expired / stacking), `mergeQuota` (unlimited / boolean / numeric-add / numeric-onto-unlimited / new), `subscribe` happy / `404` / tx-`500`, `listPayments` envelope, `downgradeExpiredUsers` count / zero. Verified green from root: `pnpm lint` (3/3), `pnpm build` (3/3; api TSC 0 issues, 54 files via SWC), `pnpm test` (**90**: web 26 + api 64, up from 49). Dep added to api: `@nestjs/schedule` ^6. **No schema/migration** — `payments`/`users`/`userPermissionQuotas` already existed; only seed data changed (re-seeded the Neon **dev** branch → 10 permissions, 9 role-permissions). Live against dev: register (free, planId 12) → subscribe Pro → `201` (planId 13, expiry now+1mo, payment `$9`/demo-tx/sslcommerz/1mo); DB confirmed quota top-up (prescriptions/patients → unlimited `-1`, `ai:use_features` 0→1, `reports:view` stays 1); no-auth → `401`; bad plan → `404`; payments as doctor → `401`, promoted to super-admin → `200` paginated; 2nd subscribe **stacked** expiry +1mo; missing `transactionId` → `400`; `limit=500` → `400`; `ScheduleModule` initialized at boot. Left the promoted super-admin test user in the dev DB. Deferred: the quota-decrement guard; real SSLCommerz/bKash gateway (separate future feature — handler currently hit directly with a demo tx); rejecting subscribing to the free plan; configurable `durationMonths`; cron not exercised live (unit-test only); `project-overview.md` still documents the old stack in places.
- **Prescription CRUD APIs** — Third business module on the NestJS API. New `apps/api/src/prescription/` (`PrescriptionModule` imports `AuthModule` for the exported guards; DB via the global `DRIZZLE` token) registered in `app.module.ts`. `PrescriptionService` (`@Inject(DRIZZLE)`, all async via `tryit` — no raw try/catch) with a pure exported helper `parsePatientIdFromSearch` (whole-string positive integer → `number | null`, so `"12"`→12 but `"12 Main St"`/`"0"`/`""`→null) and a private `buildPrescriptionValues` shared by create/update (maps DTO → columns; `followUpDate` ISO string → `Date`; leaves array/jsonb columns `undefined` so the DB defaults to `[]`). Methods: `createPrescription(userId, dto)` — **combined patient+prescription create** per spec: if `patientId` is supplied, verify it exists **and** belongs to the caller (→`404` else); if absent, the conditional-validated patient fields are inserted first; then a single `db.transaction` (existing patient is re-selected inside the tx, or the new patient is `insert().returning()`) inserts the prescription with that `patientId` + `userId` — tx failure → `500`; returns the prescription with its `patient` embedded. `listPrescriptions(userId, {page,limit,search})` — paginated history newest-first: always `innerJoin patients`, `where userId = caller` AND (when `search`) `or(ilike phone, ilike email, [eq id if numeric])`; `count()` + rows query, returns `{data,page,limit,total,totalPages}` with each row `{...prescription, patient}`. `getPrescription(userId, id)` (joined, scoped → `404`), `updatePrescription(userId, id, dto)` (existence/ownership check → `404`, then `update().set(buildPrescriptionValues)` — Drizzle skips `undefined` keys so it's a true partial update, re-fetch), `deletePrescription(userId, id)` (`.returning({id})` scoped → `404`). `PrescriptionController` `@Controller("prescriptions")` with **class-level** `@UseGuards(AccessTokenGuard, PermissionsGuard)`, each route `@RequirePermission(...)` + `@GetUser().userId`: `POST` (`201`, `PRESCRIPTIONS_CREATE`) / `GET` list (`PRESCRIPTIONS_READ`) / `GET :id` (`PRESCRIPTIONS_READ`) / `PATCH :id` (`PRESCRIPTIONS_UPDATE`) / `DELETE :id` (`200`, `PRESCRIPTIONS_DELETE`); `:id` is the text UUID PK (no `ParseIntPipe`). DTOs (class-validator): `CreatePrescriptionDto` (optional `patientId`; patient fields `name`/`phone`/`age`/`gender` gated by `@ValidateIf(o => o.patientId === undefined)` + a service-side backstop → `400`; `email`/`address` optional; prescription fields all optional — `medicines` nested `@ValidateNested`+`@Type(() => MedicineEntryInput)`, `followUpDate` `@IsDateString`, `status`/`gender`/`foodTiming` via `@IsIn`), `UpdatePrescriptionDto` (prescription fields only, all optional — patient never edited here), `ListPrescriptionsDto` (`search?` + `@Type(()=>Number)` `page`≥1 / `limit` 1–100 defaults 1/20), `MedicineEntryInput`. RBAC: added three **boolean** keys — `prescriptions:read`/`update`/`delete` — to the `Permission` enum + `PERMISSION_CATALOG`, granted to the **doctor** role in `seed.ts`'s `ROLE_PERMISSION_SEED` (resolved via the guard's role-fallback since doctors have no quota row for them); `prescriptions:create` (the existing **numeric** plan quota, free 15 / pro unlimited) is reused for create. **Decisions (user-approved):** create gated on `prescriptions:create` only (patient creation rides along — no separate `patients:create` check, since the guard checks one permission per route); a single `?search=` term over patient phone/email/id (OR semantics + substring `ilike`, so the id match isn't exclusive — `?search=1` also matches phones containing "1"); patient embedded in list/get. Tests: **13 new** Vitest (`prescription.service.test.ts`, same chainable-thenable Drizzle stub keyed by table identity + `joined`/`ordered`/`limited` flags to disambiguate the count vs list vs get-one `select().from(prescriptions)` shapes): `parsePatientIdFromSearch` (2), create reuse-patient / bad-patientId-`404` / new-patient / tx-`500`, list envelope, get happy / `404`, update happy / `404`, delete happy / `404`. Verified green from root: `pnpm lint` (3/3), `pnpm build` (3/3; api TSC 0 issues, 62 files via SWC), `pnpm test` (**103**: web 26 + api 77, up from 90). **No schema/migration** — `prescriptions`/`patients` already existed; only seed data changed (re-seeded the Neon **dev** branch → 13 permissions, 12 role-permissions). Live against dev: no-auth → `401`; register doctor → create-without-patientId creates patient + prescription (patient embedded); create-with-existing-patientId reuses it; bad `patientId` → `404`; missing patient fields (no `patientId`) → `400`; list paginated envelope (patient embedded); search by phone/email/id substrings (no-match → `total 0`); `GET :id` → `200` / unknown → `404`; `PATCH` status→Printed + notes → `200` / invalid status → `400`; `DELETE` → `200 {id}` / re-`DELETE` → `404` / `GET` deleted → `404`; **per-doctor isolation confirmed** — a 2nd doctor's list is empty, and `GET`/`DELETE` of the 1st doctor's prescription → `404` while the owner still gets `200`. Left two registered test doctors + their data in the dev DB. Deferred: the quota-decrement guard (create doesn't yet decrement `prescriptions:create`); a `patients`-only CRUD module (registry/search beyond the create-on-prescription path); narrowing search so a numeric term matches the patient id exclusively; `project-overview.md` still documents the old stack in places.
- **Patient CRUD APIs** — Fourth business module on the NestJS API, closely mirroring the prescription module. New `apps/api/src/patient/` (`PatientModule` imports `AuthModule` for the exported guards; DB via the global `DRIZZLE` token) registered in `app.module.ts`. **No create endpoint** — patients are created through `POST /prescriptions` (the builder submits patient + prescription together), so this is read/update/delete only. `PatientService` (`@Inject(DRIZZLE)`, all async via `tryit` — no raw try/catch) with the same pure exported helper `parsePatientIdFromSearch` (whole-string positive integer → `number | null`): `listPatients(userId, {page,limit,search})` — paginated registry newest-first, `where userId = caller` AND (when `search`) `or(ilike phone, ilike email, [eq id if numeric])`; `count()` + rows query → `{data,page,limit,total,totalPages}`; `getPatient(userId, id)` (scoped → `404`), `updatePatient(userId, id, dto)` (existence/ownership check → `404`, then `update().set({...})` — Drizzle skips `undefined` keys for a true partial update, re-fetch), `deletePatient(userId, id)` (`.returning({id})` scoped → `404`; the patient's prescriptions cascade via FK). `PatientController` `@Controller("patients")` with **class-level** `@UseGuards(AccessTokenGuard, PermissionsGuard)`, each route `@RequirePermission(...)` + `@GetUser().userId`: `GET` list (`PATIENTS_READ`) / `GET :id` (`PATIENTS_READ`) / `PATCH :id` (`PATIENTS_UPDATE`) / `DELETE :id` (`200`, `PATIENTS_DELETE`); `:id` via `ParseIntPipe` (serial integer PK, unlike prescriptions' text UUID). DTOs (class-validator): `ListPatientsDto` (`search?` + `@Type(()=>Number)` `page`≥1 / `limit` 1–100 defaults 1/20, identical to `ListPrescriptionsDto`) + `UpdatePatientDto` (all optional — `name`/`phone`/`email`/`address` strings, `age` `@IsInt`/`@Min(0)`, `gender` `@IsIn(["Male","Female","Other"])`; owner `userId` never editable). RBAC: added three **boolean** keys — `patients:read`/`update`/`delete` — to the `Permission` enum + `PERMISSION_CATALOG`, granted to the **doctor** role in `seed.ts`'s `ROLE_PERMISSION_SEED` (resolved via the guard's role-fallback since doctors have no quota row for them); `patients:create` (the existing **numeric** plan quota, free 50 / pro unlimited) is reused for create-via-prescription. Tests: **9 new** Vitest (`patient.service.test.ts`, same chainable-thenable Drizzle stub keyed by table identity + `ordered`/`limited` flags to disambiguate the three `select().from(patients)` shapes — count / list / get-one): `parsePatientIdFromSearch` (2), list envelope, get happy / `404`, update happy / `404`, delete happy / `404`. Verified green from root: `pnpm lint` (3/3), `pnpm build` (3/3; api TSC 0 issues, 68 files via SWC), `pnpm test` (**112**: web 26 + api 86, up from 103). **No schema/migration** — `patients` already existed; only seed data changed (re-seeded the Neon **dev** branch → 16 permissions, 15 role-permissions). Live against dev: no-auth → `401`; register doctor → created 2 patients via `POST /prescriptions`; list (paginated envelope, 2 patients); search by phone/email/id substrings (no-match → `total 0`); `GET :id` → `200` / unknown → `404` / non-integer `:id` → `400` (ParseIntPipe); `PATCH` age+address → `200` / invalid gender → `400`; `DELETE` → `200 {id}` / re-`DELETE` → `404` / `GET` deleted → `404`; **cascade confirmed** — deleting a patient removed their prescriptions (prescription search by that phone → `total 0`); **per-doctor isolation confirmed** — a 2nd doctor's list is empty, and `GET`/`DELETE` of the 1st doctor's patient → `404` while the owner still gets `200`. Left test doctors + their data in the dev DB. Deferred: the quota-decrement guard; narrowing search so a numeric term matches the patient id exclusively; `project-overview.md` still documents the old stack in places.
- **Auth Phase 2 — Who Am I + Sign Out** — Two API-only endpoints on the existing Passport/JWT `AuthController`/`AuthService` (no new module, no schema/migration, no new deps). `AuthService.getMe(userId)` selects the fresh `users` row by id via `tryit` (the JWT payload only carries `id`/`email`, so the profile must be re-read) → `401 UnauthorizedException` if the row is gone (token references a deleted user), `500` on query error. `AuthService.logout(userId)` nulls `users.refreshToken` via `tryit` (mirrors the reset-password session-invalidation), `500` on error. Controller: `GET /auth/me` (`@UseGuards(AccessTokenGuard)`, `@GetUser() authUser: AuthUser` → `getMe(authUser.userId)`, returns the same allowlist as register/login — `id`/`name`/`email`/`isOnboarded`/`planId`/`createdAt`, **no** password/tokens; plain JSON return, no `@Res`); `POST /auth/logout` (`@HttpCode(200)` + `AccessTokenGuard` + `@Res()`) calls `logout`, then `res.clearCookie("accessToken"/"refreshToken", { httpOnly:true, secure:false })` to match how the cookies were set, and returns `{ message: "Signed out successfully." }`. Reused the existing `AccessTokenGuard` (already an `AuthModule` provider — spec said "add it if the api needs it", but it was already present) which silently rotates an expired access token via the refresh cookie before the handler runs. Imported `AuthUser` from `auth.types` and `AccessTokenGuard` into the controller. Tests: **5 new** Vitest — service: `getMe` happy (returns the row) / missing-user → 401; `logout` clears the refresh token (asserted via the stub's `refreshTokenUpdate` capture = `{ refreshToken: null }`); controller: `me` returns only the safe allowlist (no password/refreshToken), `logout` clears both cookies (asserted `clearCookie` args) + returns the message (new `clearCookie` mock on the res stub). Verified green from root: `pnpm lint` (3/3), `pnpm build` (3/3; api TSC 0 issues, 68 files via SWC), `pnpm test` (**117**: web 26 + api 91, up from 112). Live against Neon **dev**: register → `GET /auth/me` with cookies → `200` safe body, without cookies → `401`; `POST /auth/logout` → `200` + both `Set-Cookie` expired to epoch (`Expires=Thu, 01 Jan 1970`), logout without auth → `401`; crucially, replaying **only** the stale refresh cookie (forcing the guard's rotation path) → `401`, proving the nulled refresh token invalidates the session server-side (the short-lived access JWT stays valid until its own expiry, as expected for stateless access tokens). Decisions: missing-user surfaces as `401` (not `404` — the spec left it open); logout returns a generic success even for an already-signed-out caller (the guard already gates it to authenticated callers). Committed the feature spec (`context/features/auth-phase-2.spec.md`). Deferred: rate-limiting; a web sign-out/profile UI; `project-overview.md` still documents the old stack in places.
- **User Management APIs (admin)** — Fifth business module on the NestJS API — the first **admin-facing** one (operates across all users, unlike the doctor-scoped patient/prescription modules). New `apps/api/src/user/` (`UserModule` imports `AuthModule` for the exported guards; DB via the global `DRIZZLE` token) registered in `app.module.ts`. `UserService` (`@Inject(DRIZZLE)`, all async via `tryit` — no raw try/catch) with a module-level `USER_PUBLIC_COLUMNS` allowlist (never the password hash / refresh token / reset columns) and a private `assertRoleExists` (→ `400 BadRequestException` if the `roleId` is unknown, shared by create + update): `createUser(dto)` — validate role → pre-check duplicate email (`409 ConflictException`) → bcrypt-hash (salt 10) → one `db.transaction` inserts the `users` row (`provider:"credentials"`, **no `planId`/quota** — role-only) + the `userRoles` row, then re-fetch via `getUser`; `listUsers({page,limit,search})` — paginated newest-first, optional `search` is one term OR-matched (substring `ilike`) across `name`/`contactNumber`/`email`, `count()` + rows → `{data,page,limit,total,totalPages}` (safe columns); `getUser(id)` (safe columns, `404` if missing); `updateUser(id,dto)` — existence check → `404`, optional email-change clash check (`ne(users.id,id)` → `409`), optional role validation, optional bcrypt password reset, then one `db.transaction` updates the user fields and — when `roleId` is supplied — **replaces** the role (delete `userRoles` for the user + insert the new one), re-fetch; `deleteUser(id)` — hard delete `.returning({id})` → `404` if no row (roles/quotas/patients/prescriptions cascade via FKs; payments `set null`). `UserController` `@Controller("users")` with **class-level** `@UseGuards(AccessTokenGuard, PermissionsGuard)`, each route `@RequirePermission(...)`, **not** `@GetUser`-scoped (admin operates across all users): `POST` (`201`, `USERS_CREATE`) / `GET` list (`USERS_READ`) / `GET :id` (`USERS_READ`) / `PATCH :id` (`USERS_UPDATE`) / `DELETE :id` (`200`, `USERS_DELETE`); `:id` is the text UUID PK (no `ParseIntPipe`). DTOs (class-validator): `CreateUserDto` (name 2–255, `@IsEmail`, password 8–72, `roleId` `@IsInt` required, optional `contactNumber`/`qualifications`/`bmdcRegNo`/`clinic*`), `UpdateUserDto` (all optional — same fields + `isOnboarded` `@IsBoolean` + optional `roleId` for role swap + optional `password` for admin reset), `ListUsersDto` (`search?` + `@Type(()=>Number)` `page`≥1 / `limit` 1–100 defaults 1/20, identical to the other list DTOs). RBAC: added four **boolean** keys — `users:create`/`read`/`update`/`delete` — to the `Permission` enum + `PERMISSION_CATALOG`, granted to the **`super-admin` role only** in `seed.ts`'s `ROLE_PERMISSION_SEED` (role-capabilities → resolved via the guard's role-fallback branch, like the `plans:*` keys; a plain `doctor` hits the role miss → `401`). **Decisions (user-approved):** create takes a **password** in the payload (admin-set, so the user can log in immediately); **role-only, no plan/quota** snapshot (admin/internal users don't need billing entitlements — `planId` stays null); a **single combined `?search`** over name/contactNumber/email (matches the existing list convention vs. discrete params); **update can swap the role** via optional `roleId`. Tests: **10 new** Vitest (`user.service.test.ts`, same chainable-thenable Drizzle stub keyed by table identity + the **selected columns** — `value`→count, single `id`→existence/clash check, full row→get — plus `orderBy` for list-vs-get, and a bcrypt `hash` mock): `createUser` happy (role assigned, safe body, `bcrypt.hash` called, no password leak) / bad-role-`400` / duplicate-`409`, `listUsers` envelope, `getUser` happy / `404`, `updateUser` happy / `404`, `deleteUser` happy / `404`. Verified green from root: `pnpm lint` (3/3), `pnpm build` (3/3; api TSC 0 issues, 75 files via SWC), `pnpm test` (**127**: web 26 + api 101, up from 91). **No schema/migration** — `users`/`roles`/`userRoles` already existed; only seed data changed (re-seeded the Neon **dev** branch → **20 permissions, 19 role-permissions**). Live against dev: no-auth → `401`; plain doctor → `401` (role lacks `users:create`); promoted a user to super-admin then `POST` `201` (`planId:null`, no password in body, `provider:"credentials"`, and the created user can **log in** with the admin-set password) → invalid role → `400` → short password → `400` (ValidationPipe) → duplicate email → `409`; `GET` list paginated envelope; `?search=` by name / contactNumber / email substrings each → `total 1`, no-match → `total 0`; `GET :id` → `200` / unknown → `404`; `PATCH` name+`isOnboarded`+role-swap(17→18, **DB-confirmed** `user_roles` now only `[18]`)+password-reset → `200` (old password → `401`, new → `200`) / invalid role → `400` / unknown user → `404`; `DELETE` → `200 {id}` → **cascade confirmed** (`user_roles` row gone) → re-`DELETE` / `GET` deleted → `404`. Committed the feature spec (`context/features/user.spec.md`). Left a plain doctor + a promoted super-admin test user in the dev DB. Deferred: a role-picker frontend / web admin UI (separate later feature, per spec); rate-limiting; `project-overview.md` still documents the old stack in places.
- **Analytics APIs (dashboard)** — Sixth business module on the NestJS API — read-only dashboard analytics, the first module guarded by **`AccessTokenGuard` alone** (no `PermissionsGuard`/`@RequirePermission`, per spec) and the first to use **raw SQL aggregation** + `db.execute`. New `apps/api/src/analytic/` (`AnalyticModule` imports `AuthModule` for the guard; DB via the global `DRIZZLE` token) registered in `app.module.ts`. `AnalyticService` (`@Inject(DRIZZLE)`, all async via `tryit` — no raw try/catch) with two pure exported helpers `startOfToday(now)` / `startOfMonth(now)` (local-time midnight bounds) + a `TOP_DIAGNOSES_LIMIT = 5` const: `getSummary(userId)` — the three dashboard stat cards in one response via `Promise.all` under a single `tryit`: **Today's Patients** = `countDistinct(prescriptions.patientId)` for prescriptions `createdAt >= startOfToday` (distinct patients *seen* today, not registered), **Prescriptions This Month** = `count()` since `startOfMonth`, **Total Patients** = `count()` of the doctor's `patients` (each `.then(rows => rows[0]?.value ?? 0)`); `getTopDiagnoses(userId)` — most-used diagnosis tags this month via `db.execute<TopDiagnosis>(sql\`… FROM ${prescriptions}, unnest(${prescriptions.diagnosisTags}) AS tag WHERE user_id = … AND created_at >= ${monthStart} GROUP BY tag ORDER BY count DESC, tag ASC LIMIT 5\`)` returning `result.rows` (`{label,count}[]`; `count(*)::int` so pg returns a JS number, not a bigint string). Both methods scoped to the doctor; query errors → `500 InternalServerErrorException`. `AnalyticController` `@Controller("analytics")` with **class-level** `@UseGuards(AccessTokenGuard)` + `@GetUser().userId`: `GET /analytics/summary` + `GET /analytics/top-diagnoses` (both plain JSON returns, no DTOs/params). **Decisions (user-approved):** _Today's Patients_ = distinct patients with a prescription today (vs. patients registered today); **`AccessTokenGuard` only** (the seeded `reports:view` permission was *not* layered on — kept to the literal spec); **calendar month / top 5** (matches the frontend's "This Month" badge + 5-bar chart). **Build gotcha:** `TopDiagnosis` is a **`type` alias, not an `interface`**, so it satisfies the `Record<string, unknown>` constraint on `db.execute<T>()` (interfaces lack the implicit index signature → TS2344). Tests: **6 new** Vitest (`analytic.service.test.ts`, a lighter stub than the other modules — `getSummary`'s three `select().from().where()` thenables are disambiguated purely by **call order** via a `selectIndex` counter returning `summaryValues[i]`; `getTopDiagnoses` uses an `execute()` returning a pg-style `{ rows }`; thenable-`reject` flags for the error paths): `startOfToday`, `startOfMonth`, `getSummary` happy (3 counts → 3 cards) / `500`, `getTopDiagnoses` happy / `500`. Verified green from root: `pnpm lint` (3/3), `pnpm build` (3/3; api TSC 0 issues, 79 files via SWC), `pnpm test` (**133**: web 26 + api 107, up from 101). **No schema/migration, no new deps, no seed change** (read-only over existing `patients`/`prescriptions`; AccessTokenGuard-only means no new permission keys). Live against the running dev server (hot-reloaded) on Neon **dev**: register doctor → 2 prescriptions (Alice: 2 tags, Bob: 1 tag, both new patients) → `GET /analytics/summary` → `{"todaysPatients":2,"prescriptionsThisMonth":2,"totalPatients":2}`; `GET /analytics/top-diagnoses` → `[{"label":"Viral Fever","count":2},{"label":"Hypertension Stage 1","count":1}]` (unnest aggregation + desc sort correct); no-auth → `401`. Committed the feature spec (`context/features/analytics-api.spec.md`; its body had a `@apps/api/src/user` typo — built the `analytic` module per the title/requirement). Left a test doctor + 2 patients in the dev DB. Deferred: wiring the web dashboard cards (still static seed in `apps/web/.../data.ts`) to these endpoints (out of scope, API-only); optionally layering `reports:view` via `PermissionsGuard`; a rolling-window variant; most-prescribed-medicines analytics; `project-overview.md` still documents the old stack in places.
- **Auth Pages (web)** — First web feature to wire `apps/web` to the NestJS auth API; built the four auth pages + symmetric route guards + the sign-out UI. New App Router pages (all server components that run a redirect guard then render an on-brand card): `/sign-in` (login), `/sign-up` (register), `/forgot-password`, `/reset-password` (reads `?token=` via `searchParams`). Shared in `src/components/auth/`: `AuthShell` (centered card reusing the landing design system — `Logo`, paper backdrop, DM Sans/serif, navy/teal tokens, panel shadow), `GoogleButton` (full-page `<a>` to `${API}/auth/google` — redirect flow, not fetch; inline brand-accurate Google "G" SVG), `form-parts` (`Field`/`OrDivider`/`FormAlert`/`FormSuccess`), `PasswordField` (show/hide eye toggle; `Omit<…,"type">` so visibility controls the input type; forwards RHF register props incl. ref via React-19 ref-as-prop), and four `"use client"` RHF + `zodResolver` forms. Forms `fetch` the API with `credentials: "include"` (httpOnly cookies; CORS `credentials:true` already set for the web origin), surface API errors via a top-of-form alert (`extractApiError` picks the first class-validator message), and on success `router.replace("/dashboard")` + `refresh()`. forgot-password shows the API's generic no-enumeration message; reset-password adds a client-side confirm-password field (refined zod) and shows an error state when `?token=` is absent. Lib: `lib/api.ts` (`API_URL` from `NEXT_PUBLIC_API_URL`, `AuthUser`, `extractApiError`), `lib/auth-schemas.ts` (zod schemas, bounds mirror the API DTOs — email, password 8–72, name 2–255), `lib/auth.ts` (`"server-only"`: `getCurrentUser` forwards the `accessToken`/`refreshToken` cookies to `GET /auth/me`; `redirectIfAuthenticated` for the auth pages; `requireAuth` for protected pages). **Route guards (symmetric):** the four auth pages redirect a signed-in user → `/dashboard`; the dashboard `layout.tsx` was made `async` and calls `await requireAuth()` → a signed-out user is bounced to `/sign-in` (covers all `/dashboard/*` in one place; routes became dynamic `ƒ` since they read cookies). **Sign-out UI:** new `dashboard/SidebarUserMenu.tsx` replaces the static doctor card — clicking the name opens an upward popup (outside-click/Escape to close) with a Sign out action that `POST`s `/auth/logout` (`credentials:"include"`) then `window.location.href = "/"` (full nav resets client state); errors surface via the existing dashboard toast. Repointed the landing CTAs (were a dead `/api/auth/signin` NextAuth path): `SIGN_IN_HREF`→`/sign-in`, added `SIGN_UP_HREF`→`/sign-up` and wired the "Start free"/"Create account"/pricing CTAs in `SiteHeader`/`Hero`/`Pricing`/`FinalCta`/footer. **Decisions (user-approved):** routes `/sign-in` + `/sign-up` (per project-overview Routes table, over the spec's "login/register" wording); **server-component** redirect guard (no flash, over a client `/auth/me` check); reuse RHF + zod (already in web). Env: added `NEXT_PUBLIC_API_URL` (http://localhost:3001) to web `.env` + new `.env.example`. Tests: **13 new** Vitest (`lib/auth-schemas.test.ts`): the four zod schemas (valid/invalid email, password 8–72 bounds, name min 2, reset confirm-mismatch on the right path) + `extractApiError` (array→first, string as-is, fallback). Verified green from root: `pnpm lint` (3/3), `pnpm build` (3/3; web 4 new dynamic routes, api TSC 0 issues), `pnpm test` (**146**: web 26→39 + api 107). Live against Neon **dev** + both dev servers: all four pages render with the right fields + Google link; redirect guard — `/sign-in`+`/sign-up` with a session → `307 /dashboard`, without → `200`; all `/dashboard/*` without a session → `307 /sign-in`, with → `200`; cross-origin credentialed login from `Origin: localhost:3000` → CORS `Allow-Origin`/`Allow-Credentials:true` + both httpOnly cookies, wrong password → `401`; logout → `200` + cookies expired to epoch + `/auth/me` after → `401`. **No api changes** (consumed existing endpoints), no schema/migration, no new deps (used web's existing RHF/zod/lucide). Committed the feature spec (`context/features/auth-pages.spec.md`). Deferred: in-browser interactive verification of the JS submit→fetch→client-redirect (verified every piece over HTTP, not driven through a real browser); an error-variant for the dashboard `Toaster` (currently always renders a green check, so a rare logout failure shows the right text with a success icon); rate-limiting; `project-overview.md` still documents the old stack in places.
- **Dynamic Landing Page (auth-aware CTAs + DB-driven plans)** — Second web feature; made the marketing landing page (`/`) both database-driven and session-aware, consuming the existing public `GET /plans` plus the existing `getCurrentUser()`. New `apps/web/src/lib/plans.ts`: `fetchPlans()` (server fetch of `GET ${API_URL}/plans`, `cache:"no-store"`, returns `[]` on any error so the page degrades gracefully — never crashes on a backend hiccup), the pure `resolvePlanCta(plan, user, {signupHref, signupLabel, dashboardHref, currentPrice})` decision fn (signed-out → marketing CTA → `/sign-up`; current plan → disabled "Current plan"; pricier than current → "Upgrade"; cheaper → "Downgrade"; same price non-current → "Switch plan"; signed-in but current plan/price unknown, e.g. admin `planId:null` → neutral "Go to dashboard"; all switch buttons → `/dashboard`), and `deriveBullets(permissions)` (fallback feature bullets from a plan's permission grants — `false` skipped, `true`→label, `unlimited`→"Unlimited …", numeric→"Up to N …" — used only for slugs without curated copy). `Pricing.tsx` rewritten as an **`async` server component**: fetches plans, computes `currentPrice` (price of the user's `planId` among the fetched plans, else null), renders each plan via a `PlanCard` subcomponent — **odd-index → navy gradient, even-index → white** (`index % 2 === 1`), price from `priceMonthly` (`$N` + "/ forever" when 0 else "/ month"), bullets from a slug-keyed `PLAN_COPY` map (fallback `deriveBullets`), and the resolved CTA as a disabled `<button>` (Current plan) or a `<Link>` button. `data.ts`: replaced the static `PRICING`/`PricingTier` with `PLAN_COPY: Record<slug, {desc, cta, badge?, bullets}>` (curated marketing copy; price/name/existence now come from the DB) + added `DASHBOARD_HREF = "/dashboard"`. `app/page.tsx` made **`async`**: resolves the session once via `getCurrentUser()` and fans `authed`/`user` to the sections (the route is now dynamic `ƒ` since it reads cookies). Auth-aware primary CTAs — `SiteHeader` (desktop + mobile drawer), `Hero`, `FinalCta`, `Footer` each take an `authed` prop and swap sign-in/sign-up CTAs for a single "Go to dashboard" → `/dashboard`; `Footer` collapses the "Sign in"/"Create account" links in the Get-started column into one dashboard link (keeping "Book a demo"). **Decisions (user-approved):** feature bullets via **slug→copy map + permission-derived fallback** (DB has no marketing copy); **literal odd-index→blue** highlight (over a price-driven highlight); **all** auth-aware buttons + Upgrade/Downgrade route to **`/dashboard`** (no web billing page exists yet). Tests: **8 new** Vitest (`lib/plans.test.ts`): `deriveBullets` (formatting + unknown-key fallback) and `resolvePlanCta` (all six branches). Verified green from root: `pnpm lint` (3/3), `pnpm build` (3/3; `/` now `ƒ` dynamic, all 12 web routes build), `pnpm test` (**154**: web 39→47 + api 107). Live against the running dev servers on Neon **dev**: `GET /plans` returns Free($0)/Pro($9); signed-out landing → "Start free"/"Go Pro" → `/sign-up`, "Sign in" present; signed-in **on Free** (registered fresh, `planId:18`) → all CTAs "Go to dashboard", Free card "Current plan" (disabled), Pro card "Upgrade", no "Sign in"; after subscribing to Pro (`planId:19`) → Pro card "Current plan" (disabled), Free card "Downgrade", no "Upgrade"; the only remaining "Start free" is the static section headline ("Start free. Go unlimited…"), and apparent double counts were just RSC flight-data copies (one rendered `<a>`). **No API changes** (consumed existing public `GET /plans` + `getCurrentUser`/`GET /auth/me`), no schema/migration, no new deps. Left a Pro-subscribed test user in the dev DB. Deferred: a real web billing/subscribe page (Upgrade/Downgrade just route to `/dashboard` for now); revalidating/caching the plans fetch (currently `no-store`); `project-overview.md` still documents the old stack in places.
- **Billing Page (web) + auth-aware plan CTAs + OAuth plan provisioning** — Third web feature; built the in-dashboard billing surface, made plan CTAs subscription-aware end-to-end (calling the real subscribe API), and fixed a backend bug where Google sign-up never assigned a plan/role. **Web — billing page:** new **Billing** sidebar item (System section, `CreditCard` icon) + `PAGE_TITLES` entry in `dashboard/data.ts`; new route `app/dashboard/billing/page.tsx` (async server component: `getCurrentUser()` + `fetchPlans()`, resolves the current plan/price, renders a navy-gradient current-plan summary + a "Plans" grid + a "payments are simulated" prototype note). New client `BillingPlans.tsx` renders dashboard-styled `Card`s per plan (slug-keyed `PLAN_COPY` bullets / `deriveBullets` fallback, `Current` badge + navy ring on the active plan) and wires each card's CTA to the dashboard `toast`. **Web — shared subscribe action:** new client `PlanActionButton.tsx` used by **both** the billing page and the landing pricing — disabled/`link` CTAs render plainly; a `subscribe` CTA `POST`s `/billing/subscribe` (`credentials:"include"`, demo `transactionId: demo_${Date.now()}`), then `router.push("/dashboard/billing")` + `refresh()`; inline error fallback + optional `onResult` toast hook. **Web — landing CTAs:** reworked `resolvePlanCta` (`lib/plans.ts`) to return `{ label, disabled, action: "link"|"subscribe", href, planSlug }` and added `FREE_PLAN_SLUG` — signed-out → marketing link to sign-up; current **free** plan → **"Active"** (disabled); current **paid** plan → **"Increase limit"** (subscribe top-up); pricier → **"Upgrade"**, cheaper → **"Downgrade"**, same-price → "Switch plan" (all subscribe); admin/unknown plan → neutral "Go to dashboard". `Pricing.tsx` PlanCard now renders `PlanActionButton` (dropped the inline `Button`/`Link` branching). **API — free re-subscribe guard:** `BillingService.subscribe` now also selects the user's `planId` and throws **400** when the target is the free plan and the user is already on it (`plan.slug === FREE_PLAN_SLUG && currentUser.planId === plan.id`) — closes the "free 15→30 credit" doubling; paid top-ups/upgrades/downgrades unaffected. **API — OAuth provisioning fix (root cause of "No active plan"):** `validateSocialUser` previously upserted only name/email/provider, so every Google doctor was orphaned (`plan_id` null, no role) → billing showed "No active plan" and CTAs fell back to "Go to dashboard". Extracted shared private helpers `resolveSignupDefaults()` (free plan + doctor role + free plan-permissions, 500 if unseeded) and `createUserWithFreePlan(values, defaults)` (one-tx insert user+`planId` / `userRoles` / quota snapshot), now used by **both** `registerUser` and `validateSocialUser` so the two sign-up paths can't drift. `validateSocialUser` now: **new** OAuth user → provisioned like registration; **existing but unprovisioned** (null plan) → **self-healed** via `healUserToFreePlan` (idempotent: set plan + `onConflictDoNothing` role + `onConflictDoUpdate` quotas) so the 12 legacy orphaned Google accounts heal on next login (no risky bulk DB edit); **already-provisioned** user → only `provider` refreshed (Pro stays Pro). Admin-created users (User module, credentials, intentional null plan) are untouched and still correctly show "No active plan". **Decisions (user-approved):** Billing item in the System nav group; frictionless demo subscribe (real SSLCommerz/bKash deferred); block free re-subscribe only while already on free (Pro→free downgrade still allowed); heal-on-login over a bulk backfill. Tests: **+11** — web `plans.test.ts` rewritten for the new CTA shape + Active/Increase-limit/Upgrade/Downgrade branches (8); api `billing.service.test.ts` free-re-subscribe→400 (1, with `planId` added to the mock currentUser); api `auth.service.test.ts` 3 new `validateSocialUser` cases (new-user provision / heal-null-plan / already-provisioned passthrough) — extended the Drizzle stub with `onConflictDoNothing`/`onConflictDoUpdate`/update-`returning` + a `planUpdate` capture. Verified green from root: `pnpm lint` (3/3), `pnpm build` (3/3; new `/dashboard/billing` route builds), `pnpm test` (**159**: web 48 + api 111, up from 133). Live against Neon **dev**: free user re-subscribing to Free → `400` (correct message), Pro subscribe → `201`, Pro re-subscribe stacks expiry +1mo (Increase-limit path); registration regression after the helper refactor → free `planId` + Pro subscribe `201` (proves role+quotas provisioned). **No schema/migration, no new deps.** Browser-driven verification of the Google OAuth heal path is the user's to confirm (OAuth redirect isn't curl-able; covered by unit tests + shared code path) — the user must sign out and sign in with Google again to heal their current orphaned account. Left test users in the dev DB. Deferred: real payment gateway; the quota-decrement guard; optional bulk backfill of remaining orphans (they self-heal on login); narrowing Pro→free downgrade semantics (numeric-onto-unlimited still keeps unlimited); `project-overview.md` still documents the old stack in places.
- **Dashboard Analytics (web ← API)** — Fourth web feature; wired the dashboard (`/dashboard`) stat cards + Top Diagnoses to the live analytics API (`apps/api/src/analytic`) instead of static seed. New `apps/web/src/lib/analytics.ts` (`server-only`): `fetchDashboardAnalytics()` reads the `accessToken`/`refreshToken` cookies (same forward pattern as `lib/auth.ts`'s `getCurrentUser` — factored into a private `authCookieHeader()`), then fetches `GET /analytics/summary` (`{todaysPatients, prescriptionsThisMonth, totalPatients}`) and `GET /analytics/top-diagnoses` (`{label,count}[]`) **in parallel via `Promise.all`** (`cache:"no-store"`), with a private `fetchAnalytic<T>` that returns a safe fallback (zeros / `[]`) per-endpoint on any error so the page **never crashes** on a backend hiccup, and returns `{summary, topDiagnoses}`; signed-out (no cookies) → empty fallback without a round-trip. `app/dashboard/page.tsx` made **`async`**: awaits the parallel fetch, builds stat cards via `buildDashboardStats(summary)`, colors diagnoses via `withDxColors(topDiagnoses)`, and the footer is now dynamic (`Based on {prescriptionsThisMonth} consultations`) — the route becomes dynamic (`ƒ`) since it reads cookies. `components/dashboard/data.ts`: added a `DX_COLORS` palette (reused by the existing static `TOP_DX`) + `buildDashboardStats({...})` (a `STAT_CARD_STYLES` config — labels/icons/tone classes — merged with the live numbers, `toLocaleString()` for thousands); **removed the now-dead static `DASHBOARD_STATS`** export. `components/dashboard/helpers.ts`: new pure `withDxColors(items)` that cycles `DX_COLORS` onto the API's `{label,count}` rows → `TopDx[]`. `TopDiagnoses.tsx`: now takes an optional `items?: TopDx[]` prop **defaulting to the static `TOP_DX`** so the **reports page (`/dashboard/reports`) stays untouched** (still static), plus an empty-state ("No diagnoses recorded this month yet."). **Decisions:** server-component parallel fetch forwarding cookies (matches `getCurrentUser`); per-endpoint graceful fallbacks (one bad endpoint doesn't blank the whole page); cycle a fixed color palette since the API returns no color; reports page left on static seed (analytics-only scope — Recent Patients table also still static seed). Tests: **+5** Vitest (`components/dashboard/helpers.test.ts`): `withDxColors` (order / cycles past palette length / empty) + `buildDashboardStats` (maps the three cards in order with `toLocaleString` / formats zeros). Verified green from root: `pnpm lint` (3/3), `pnpm build` (3/3; `/dashboard` now `ƒ` dynamic), `pnpm test` (web **53**, up 48→53; api unaffected). **No API changes** (consumed the existing `GET /analytics/*` endpoints), no schema/migration, no new deps. Live against the running dev servers on Neon **dev**: registered a doctor → 3 prescriptions (3 distinct patients, overlapping diagnoses) → `GET /analytics/summary` → `{3,3,3}`, `GET /analytics/top-diagnoses` → `[Migraine 2, Viral Fever 2, GERD 1]`; the rendered `/dashboard` HTML confirmed the three stat cards show `3 / 3 / 3` (navy/teal/indigo), the footer "Based on 3 consultations", and the Top Diagnoses bars Migraine 2 / Viral Fever 2 / GERD 1 — all live, fetched in parallel with forwarded cookies. **Environment note:** mid-verification the user had **two `nest --watch` dev stacks** running concurrently (a standalone `pnpm --filter api dev` alongside the turbo `pnpm dev`), both compiling to the same `apps/api/dist` and contending for port 3001 — this caused intermittent `500`s on `/analytics/summary` (the web fallback then briefly showed zeros) and eventually crashed both servers; verification was completed after starting a single clean `pnpm dev` stack. The committed API code is correct (proved by reproducing `getSummary`'s queries directly → `[2,2,2]`/`[3,3,3]`). Left test doctors + their data in the dev DB. Deferred: wiring the **Recent Patients** table + the **reports page** to live data (still static seed); revalidating/caching the analytics fetch (currently `no-store`); `project-overview.md` still documents the old stack in places.
- **Patients Page — Live Data, Pagination & Search (web ← API)** — Fifth web feature; wired the patient registry (`/dashboard/patients`) and the dashboard's Recent Patients table to the live patient API instead of the static `PATIENTS`/`RECENT` seed, and extended `apps/api/src/patient/` with prescription aggregates + a recent-patients endpoint. **API:** `listPatients` now `leftJoin`s `prescriptions` + `groupBy(patients.id)` so each registry row carries **`prescriptionCount`** (`count(prescriptions.id)`) and **`lastVisitDate`** (`max(prescriptions.createdAt)`, null if never seen) — the count query stays a plain `count()` over patients; the search OR-filter gained **`ilike(patients.name)`** (was phone/email/id only via `parsePatientIdFromSearch`). New `getRecentPatients(userId)` — a raw `db.execute<RecentPatient>(sql\`…\`)` using **`DISTINCT ON (patients.id)`** ordered by `prescriptions.created_at DESC` (one row per patient = their newest prescription, carrying that Rx's `diagnosisTags`), wrapped in an outer `ORDER BY lastVisitDate DESC LIMIT RECENT_PATIENTS_LIMIT (5)`, with a correlated `(SELECT count(*)::int …)` for total Rx; `INNER JOIN` so only patients with a visit appear (no pagination, per spec). New exported `PatientWithStats` + `RecentPatient` (a `type`, not `interface`, to satisfy `db.execute<T>`'s `Record<string,unknown>` constraint) interfaces. New `GET /patients/recent` controller route declared **before** `@Get(":id")` (else the `ParseIntPipe` param route 400s "recent"), `PATIENTS_READ`-guarded. **Web:** new `lib/patients.ts` (`server-only`, mirrors `lib/analytics.ts`): `fetchPatients({page,search,limit})` (builds `?page&limit&search`, `cache:"no-store"`, forwards the `accessToken`/`refreshToken` cookies, safe empty-envelope fallback) + `fetchRecentPatients()`; `PATIENTS_PAGE_SIZE = 10`; `ApiPatient`/`PatientsPage`/`RecentPatient` types (imported into client components as `import type` so the `server-only` module is erased from the client bundle). `app/dashboard/patients/page.tsx` → async server component reading `{ q?, page? }` searchParams, fetching the page, keyed by search to reseed the input. `PatientsTable` rewritten to consume server data via props (`patients`/`page`/`totalPages`/`total`) with a **500ms-debounced** URL push on typing (`useEffect` → `router.push(?q=…&page reset)`; skips when the trimmed term already equals the URL) and **Prev/Next pagination** (only shown when `totalPages > 1`); row-click still `prefillFromPatient` → builder; patient id now numeric (`#{id}`). `app/dashboard/page.tsx` fetches analytics + recent patients in parallel via `Promise.all`; `RecentPatients` now takes a live `items: RecentPatient[]` prop (Last Visit / first diagnosis + `+N` / Rx count, with an empty state). New pure `formatVisitDate` helper in `components/dashboard/helpers.ts` — formats both the API's ISO timestamp (`GET /patients`, Drizzle `…Z`) **and** the zoneless Postgres string raw `db.execute` returns (`GET /patients/recent`, e.g. `2026-06-05 18:37:33.722174`) by normalizing the latter to UTC (`replace(" ","T")` + append `Z`) before reading UTC parts — fixes a latent tz/day-rollover bug where the same visit could render a different day on the dashboard vs. the registry near midnight; deterministic (no hydration drift), returns "—" for null/invalid. **Decisions (user-approved):** extend the API with aggregates (vs. dropping the Last Visit/Total Rx columns); add name to the API search; dedicated `GET /patients/recent` for the dashboard (vs. reusing `?limit=5` which gives newest-*registered*, not recently-*seen*). The static `PATIENTS`/`RECENT`/`searchPatients` seed/helpers remain in `data.ts`/`helpers.ts` (still referenced by their tests; harmless exported-but-unused — not removed). Tests: **+8** — api (`patient.service.test.ts`, +2: recent-patients happy + empty; stub extended with `leftJoin`/`groupBy` [leftJoin must *not* reassign `table` so the list query stays keyed on `patients`] + an `execute` returning `{rows}`), web (`helpers.test.ts`, +4 across 2 cases: `formatVisitDate` ISO + zoneless-pg-UTC normalization + null/invalid; `searchPatients` tests untouched). Verified green from root: `pnpm lint` (3/3), `pnpm build` (3/3; api TSC 0 issues, 79 files via SWC; `/dashboard` + `/dashboard/patients` now dynamic `ƒ`), `pnpm test` (**169**: web 56 + api 113). Live against Neon **dev** (registered a doctor, created 2 patients via `POST /prescriptions`, gave one a 2nd visit): `GET /patients` → aggregates correct (Nasrin `prescriptionCount:2`, `lastVisitDate` = her newer visit; Arif `1`), pagination envelope present; `?search=arif` → name match `total 1`; `/patients/recent` → ordered by latest visit, each with its latest diagnosis (Nasrin→Dengue Fever, Arif→Hypertension Stage 1) + counts; the rendered web pages (cookie-forwarded) confirmed the registry rows + Rx badges, server-side `?q=arif` filtering, and the dashboard Recent table with latest diagnoses. **No schema/migration** (read-only over existing `patients`/`prescriptions`), **no new deps**, no seed change. Left test doctors + their data in the dev DB; stopped the dev stack after verifying (avoided the double-stack port-3001 contention noted previously). Deferred: wiring the **reports page** + the Recent table's reuse to richer data; revalidating/caching the patient fetches (currently `no-store`); narrowing the API search so a numeric term matches the patient id exclusively; the quota-decrement guard; `project-overview.md` still documents the old stack in places.
- **Patient MRN Schema + Wire Prescription Builder to API** — Persisted the frontend-generated patient identifier and made the prescription builder the first web surface to actually POST to the API (it was in-memory only). **Schema/DB:** added a `mrn` column to `apps/api/src/schemas/patient.ts` — `text("mrn").notNull()` + a **per-doctor** `uniqueIndex("patients_user_id_mrn_idx").on(userId, mrn)` (the `P-XXXXX` human id from web's `generatePatientId()`, distinct from the `serial` integer PK). Generated + applied migration `0002_moaning_centennial.sql` to the Neon **dev** branch; since the new column is NOT NULL on a populated table, first truncated the demo `patients` (prescriptions cascaded) via a **throwaway** `drizzle/cleanup-patients.ts` (user-authorized — demo data only) that was deleted after. **Decisions (user-approved):** column name `mrn`, NOT NULL, unique-per-doctor. **API — prescription create:** `CreatePrescriptionDto` gained `mrn` (required on the new-patient path via the same `@ValidateIf(patientId === undefined)` gate as the other patient fields); `PrescriptionService.createPrescription` inserts `mrn` into the new patient and added it to the validation backstop. **API — patient module:** `listPatients` search switched from numeric-id matching to **`mrn`** (now name/phone/email/mrn via `ilike`) — removed `parsePatientIdFromSearch` from `patient.service.ts` (+ its 2 tests); exposed `mrn` on `GET /patients` (select column + `PatientWithStats`) and `GET /patients/recent` (raw `db.execute` SQL + `RecentPatient` type). **Web — builder → API:** "Save to Records" now `POST`s `/prescriptions` (`credentials:"include"`), keeps the in-memory history in sync, toasts `extractApiError` on failure, clears the form, and `router.refresh()`es so the new patient shows in the registry; `phone`/`age`/`gender` are now **required** in `builderSchema` (they map to NOT NULL columns; `age` validated as a non-negative integer) with inline `Field` errors + a saving state on the button. New pure `builder/payload.ts` (`buildCreatePrescriptionPayload` + `parseFollowUpDate`): branches **new-patient** (sends `mrn` + patient fields) vs **reuse** (sends the existing integer `patientId` + prescription fields only) keyed off a new hidden `existingPatientId` form field — so loading an existing patient attaches the Rx to them instead of colliding on the unique `mrn`; maps follow-up preset → ISO date, age → number, omits empty optional strings. **Web — existing-patient flows:** `prefillFromPatient` now carries `id`/`mrn`/`email` (patient click from registry/recent loads their real `mrn` + email and reuse id, not an auto-generated id); new `builder/PatientSearch.tsx` — a debounced dropdown (mirrors the registry search) that hits `GET /patients?search=` and loads the picked existing patient into the form; registry + Recent rows now display `mrn` instead of `#id` (search label/placeholder updated to "…or MRN"). `ApiPatient`/`RecentPatient` web types gained `mrn`. Tests: web payload (new-patient + reuse branches, follow-up mapping) + tightened schema tests (required phone/age/gender, non-numeric age); api prescription/patient stubs updated for `mrn` (and the 2 dropped id-parse tests). Verified green from root: `pnpm lint` (3/3), `pnpm test` (**174**: web 63 + api 111), `pnpm build` (3/3). Live against Neon **dev**: new patient persisted with `mrn` + appears in `GET /patients`; missing `mrn` (new-patient path) → `400`; duplicate `mrn` same doctor → `500` (unique constraint, no orphan — tx rollback); **same `mrn` across two doctors → `201`** (per-doctor unique); reuse via integer `patientId` → 2nd Rx on the same patient (one row, count 2); search by mrn/name substrings; `/patients/recent` includes `mrn`. Deferred: a duplicate-`mrn`-same-doctor still surfaces as a raw **500** (web `generatePatientId` is collision-prone `P-` + 5 timestamp digits — worth a retry-on-collision or `409`); editing a loaded existing patient's demographics isn't written back on save (reuse path attaches the Rx only); the prescription-list (`prescription.service.ts`) search still uses its own numeric-id parse (unchanged); the quota-decrement guard; `project-overview.md` still documents the old stack in places.
- **Prescription History — Live Data + Sidebar User Name (web ← API)** — Sixth web feature; wired the prescription history page (`/dashboard/prescriptions/history`) to the live `GET /prescriptions` API (was a session-only in-memory store) and replaced the hardcoded sidebar doctor name with the logged-in user's real name. **Web — history page:** new `lib/prescriptions.ts` (`server-only`, mirrors `lib/patients.ts`/`lib/analytics.ts`): `fetchPrescriptions({page,search,limit})` forwards the `accessToken`/`refreshToken` cookies to `GET /prescriptions`, `cache:"no-store"`, with a safe empty-envelope fallback so the page never crashes on a backend hiccup; signed-out (no cookies) short-circuits to the fallback without a round-trip. Typed `ApiPrescription` (with its embedded `patient` + `ApiMedicine`/`ApiPrescriptionPatient`) + `PrescriptionsPage`; `PRESCRIPTIONS_PAGE_SIZE = 50` (the list has no pagination UI yet, so it pulls a generous first page — API caps `limit` at 100). New pure `builder/from-api.ts` (the reverse of `builder/payload.ts`): `prescriptionToBuilderValues(rx)` maps an API prescription → `BuilderValues`, **reusing the existing patient** (`existingPatientId` = `patient.id`, `patientId` = `patient.mrn`) so a "Repeat" save attaches a new Rx to the same patient (correct repeat-visit semantics — the old in-memory path generated a fresh MRN), a private `toMedicineValues` defaults a medicine's missing `foodTiming` to `"After"`, and `followUp` is intentionally left `""` since the builder's follow-up field is a preset dropdown (a stored timestamp can't round-trip); `toHistoryEntry(rx)` builds the display row (`formatVisitDate(createdAt)`). `from-api.ts` imports the `ApiPrescription` shape via `import type` so the `server-only` module is erased from the client bundle (its runtime mapper only runs in the server `page.tsx`). `app/dashboard/prescriptions/history/page.tsx` is now an **`async` server component** that fetches the list and passes `items` to `HistoryList`; `HistoryList.tsx` was refactored from reading the provider's in-memory `history` to consuming `items: HistoryEntry[]` via props (Repeat → `setPrefill(item.values)` + push to the builder; Print → the shared `renderRxHtml(values, profile, date)`; rows now key by `rx.id` and show `mrn` instead of the old session id). The builder already `router.refresh()`es after a save, so a newly saved Rx appears from the server immediately. **Web — sidebar name:** `DashboardProvider` gained an optional `doctorName` prop that seeds `profile.doctor` (`{ ...DEFAULT_PROFILE, doctor: doctorName }`); the dashboard `layout.tsx` (already calling `requireAuth()`) now captures the returned user and passes `doctorName={user.name}`, so `SidebarUserMenu` (and the Rx preview, which also reads `profile.doctor`) shows the real logged-in user instead of the hardcoded `DEFAULT_PROFILE.doctor` ("Dr. Sadia Rahman"). **Decisions:** server-component cookie-forwarding fetch (matches `getCurrentUser`/`lib/patients`); Repeat reuses the existing patient; keep Repeat correct over Print fidelity for follow-up (blank vs. a non-matching dropdown value). Tests: **+4** Vitest (`builder/from-api.test.ts`): `prescriptionToBuilderValues` (field mapping + reuse-patient, food-timing default, null→empty-string coercion) + `toHistoryEntry` (formatted date + builder snapshot). Verified green from root: `pnpm lint` (3/3), `pnpm test` (**178**: web 67 + api 111), `pnpm build` (3/3; `/dashboard/prescriptions/history` now dynamic `ƒ`). **No API changes** (consumed the existing `GET /prescriptions`), no schema/migration, no new deps. Live against a single clean dev stack on Neon **dev**: registered a doctor → 2 prescriptions (new patients) → `GET /prescriptions` returned the paginated envelope (patient embedded, newest-first); the rendered `/dashboard/prescriptions/history` (cookie-forwarded) showed both rows with names/MRNs/diagnosis tags (Migraine, Viral Fever, Hypertension Stage 1); the rendered `/dashboard` sidebar showed **Dr. Imran Kabir** (the registered name), not "Dr. Sadia Rahman". Left a test doctor + 2 patients in the dev DB; stopped the dev stack + cleaned temp files after. Deferred: pagination UI for the history list (currently first 50); the provider's now-unused in-memory `history`/`saveRx` left intact (the builder still writes to it) to keep the diff minimal — candidate for later cleanup; follow-up date not shown on history reprint; settings → live profile (qualification/clinic/bmdc still `DEFAULT_PROFILE`); `project-overview.md` still documents the old stack in places.
- **Role CRUD (rbac module)** — Seventh business module on the NestJS API and the last missing RBAC resource (the admin-management spec had flagged that no role module existed). New `apps/api/src/rbac/` (`RbacModule` imports `AuthModule` for the exported guards; DB via the global `DRIZZLE` token) registered in `app.module.ts`, closely mirroring **Plan CRUD** (`apps/api/src/subscription`). `RbacService` (`@Inject(DRIZZLE)`, all async via `tryit` — no raw try/catch) with a pure exported `slugify()` (local copy, like subscription's — each module owns its own to stay decoupled) and a `RoleWithPermissions` interface (`{id,name,slug,permissions: string[]}` — roles confer **boolean capabilities only**, so a grant is just the permission key, no `value`, matching the value-less `role_permissions` table): `createRole(dto)` — derive slug from name → `400` if no alphanumerics; pre-check select → `409 ConflictException` on slug collision; one `db.transaction` inserts the `roles` row + its `rolePermissions` rows together (per spec: **role + permissions created in the same call**); `getRoles(name?)` — all roles each merged in JS with their permission keys (two `.select()`s, not the relational API, to match the codebase + the test stub), with an optional `name` substring filter via `ilike`; `getRole(id)` (→ `404 NotFoundException`); `updateRole(id,dto)` (existence check → `404`; transaction updates name and, when `permissions` is supplied, **replaces** the full grant set via delete-then-insert; **slug is intentionally immutable** so the `doctor`/`super-admin` slugs registration resolves can't drift — mirrors Plan CRUD); `deleteRole(id)` (`.returning({id})` → `404`; `role_permissions` + `user_roles` cascade via FK). `RbacController` `@Controller("roles")`: `GET /roles` (optional `?name=`) + `GET /roles/:id` **public** (mirrors Plan CRUD's public reads — the spec scoped guards to Create/Update/Delete only); `POST` (`201`) / `PATCH` / `DELETE` (`200`) guarded by `@UseGuards(AccessTokenGuard, PermissionsGuard)` + `@RequirePermission(...)`; `:id` via `ParseIntPipe`. DTOs (class-validator): `CreateRoleDto` (name 2–100, `permissions` `@ArrayMinSize(1)` of `@IsEnum(Permission, {each:true})`), `UpdateRoleDto` (all `@IsOptional`, same shape), `ListRolesDto` (optional `name` string — roles are few, so the list is **not paginated**, returning every matching role like `getPlans`). RBAC: added four **boolean** keys — `roles:create`/`read`/`update`/`delete` — to the `Permission` enum + `PERMISSION_CATALOG`, granted to the **`super-admin` role only** in `seed.ts`'s `ROLE_PERMISSION_SEED` (role-capabilities → resolved via the guard's role-fallback branch, like the `plans:*` / `users:*` keys; a plain doctor hits the role miss → `401`). `roles:read` is defined + seeded for symmetry but **not applied to any route** (reads are public — same as the unused `plans:read`). **Decisions (user-approved):** auto-derive slug (not a DTO field) + immutable on update; **reads left public** (spec scoped guards to C/U/D only — offered to gate behind `roles:read` if wanted, a 2-line change). Tests: **14 new** Vitest (`rbac.service.test.ts`, same chainable-thenable Drizzle stub keyed by table identity + op as `subscription.service.test.ts`): `slugify` (3), create happy/empty-slug-`400`/duplicate-`409`/tx-error-`500`, getRoles merge, getRole happy/`404`, updateRole happy/`404`, deleteRole happy/`404`. Verified green from root: `pnpm lint` (3/3), `pnpm build` (3/3; api TSC 0 issues, 86 files via SWC), `pnpm test` (**181**: web 56 + api 125, up from 111). Re-seeded the Neon **dev** branch (now **24 permissions, 23 role-permissions**, +4 each). Live against dev: public `GET /roles` lists Doctor + Super Admin with their permissions; `?name=super` filters to one; `GET /roles/:id` unknown → `404`; `POST` no-auth → `401`; `POST` as a plain doctor → `401` (role lacks `roles:create`); promoted a test user to super-admin (via a throwaway `drizzle/promote-rbac-test.ts`, deleted after) then `POST` `201` (slug `front-desk` derived) → duplicate name `409` → invalid `permissionKey` `400` (ValidationPipe `@IsEnum`) → `PATCH` `200` (name changed to "Reception", **slug stayed `front-desk`**, perms replaced) → `DELETE` `200 {id}` → `GET`/re-`DELETE` `404`. No new deps, **no schema/migration** (`roles`/`role_permissions` already existed; only seed data + new permission keys changed). Committed the feature spec (`context/features/role-crud.spec.md`). Left one promoted super-admin test user in the dev DB. Deferred: gating reads behind `roles:read` if admin-only listing is wanted later; a web admin UI for role management (the broader admin-management spec); the quota-decrement guard; `project-overview.md` still documents the old stack in places.
- **Admin Management (web)** — First full admin web surface in `apps/web`, consuming the existing Role/Plan/User CRUD + payments-list API modules, permission-gated to super-admins. **API additions (small):** `GET /auth/me` now also returns `roles: string[]` (slugs) + `permissions: string[]` (effective keys) via new `AuthService.getAccessContext(userId)` — resolved exactly like `PermissionsGuard` (start from role-conferred capability keys, then quota rows override: `remaining !== 0` grants, `0` blocks), run in parallel with `getMe` in the `me` controller; web `AuthUser` gained optional `roles?`/`permissions?`. New **public `GET /permissions`** endpoint (`apps/api/src/rbac/permission.controller.ts`, registered in `RbacModule`) returning `PERMISSION_CATALOG` so the role/plan forms can render permission pickers from one source of truth (no drift). **Web — gating:** new permission-gated **"Administration"** sidebar section (`data.ts` `NavItem.permission` + a filter in `Sidebar.tsx` that drops items the user lacks and hides empty sections); `permissions` threaded through `DashboardProvider` ← `layout.tsx` (`user.permissions`). New `lib/auth.ts` helpers `hasPermission` + `requirePermission(key)` (server-component page guard → bounces non-admins to `/dashboard`). **Web — data/forms:** `lib/admin.ts` (`server-only` cookie-forwarding fetchers + types for roles/plans/users/payments/permissions, paginated `Page<T>` envelope), `lib/admin-schemas.ts` (zod for role/plan/user forms, bounds mirror the API DTOs; edit-user `roleId`/`password` optional since `GET /users/:id` doesn't return the role) + tests. New shadcn-style **`components/ui/dialog.tsx`** (`@radix-ui/react-dialog`, themed to the dashboard) + a shared `ConfirmDialog` + a client `adminRequest` mutation helper. **Four resources** under `/dashboard/admin/{roles,plans,users,transactions}` — each an `async` server page (`requirePermission` + parallel fetch) rendering a client manager: `RolesManager`/`RoleFormDialog` (checkbox permission picker), `PlansManager`/`PlanFormDialog` (per-permission value inputs — numeric/`unlimited` or `true`/`false` by catalog type), `UsersManager`/`UserFormDialog` (paginated + debounced search + role `<select>`, edit keeps password/role on blank), `TransactionsTable` (read-only paginated list). All follow the existing dashboard UI (Card/Table/Badge/Input/Select primitives, `save-btn`, navy/teal), toast on result, `router.refresh()` after mutations. **Seed:** updated `drizzle/seed.ts` to wipe the `users` table then insert **4 dev login accounts** — `superadmin1`/`superadmin2@mypatient.dev` (super-admin role, no plan) and `doctor1`/`doctor2@mypatient.dev` (doctor role + free plan + quota snapshot) — all sharing the bcrypt-hashed password `qwerty1234%` (local `parseQuotaValue` mirrors `AuthService`). **Decisions (user-approved):** add `GET /permissions` (vs. hardcoding the catalog); modal dialogs (shadcn) for create/edit; a new "Administration" sidebar section; seed 2+2 accounts wiping users first. Dep added to web: `@radix-ui/react-dialog`. **No schema/migration.** Verified green from root: `pnpm lint` (3/3), `pnpm test` (**206**: web 78 + api 128, +2 `getAccessContext` service tests, +1 `permission.controller` test, +11 `admin-schemas` web tests), `pnpm build` (3/3; 4 new dynamic `/dashboard/admin/*` routes). Live against Neon **dev**: `GET /permissions` → 24-key catalog; a fresh doctor's `/auth/me` has **no** admin keys, the Administration items are absent from the rendered sidebar, and `/dashboard/admin/users` → `307 → /dashboard`; a promoted super-admin's `/auth/me` has all 13 admin keys, the rendered sidebar shows Administration + all four items, the four admin pages → `200` and render their data, and Role/Plan/User **create→edit→delete** all round-trip with the exact web payloads. Seed applied to dev (4 users); `superadmin1`/`doctor1` confirmed logging in with `qwerty1234%` (super-admin role-only / doctor free plan #23). Throwaway promotion script + test rows cleaned up. Committed the feature spec (`context/features/admin-management.spec.md`). Deferred: in-browser click-through of the modal open/submit interactions (every layer verified over HTTP — rendered sidebar, page redirects, CRUD round-trips — but not driven through a real browser); surfacing each user's current role on `GET /users` so the edit form can pre-select it (currently "keep current"); narrowing the admin reads (`GET /roles`/`/plans` stay public, pages gated client/server-side); `project-overview.md` still documents the old stack in places.
