name: deploy-auth-service

on:
  push:
    branches:
      - main
    paths:
      - "3-auth-service/**"

jobs:
  build_and_push:
    runs-on: ubuntu-latest
    steps:
      - name : Transfer code
        uses: actions/checkout@v2

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./3-auth-service
          file: ./3-auth-service/Dockerfile
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/kaj-bazar-auth:latest 

  # deploy:
  #   runs-on: ubuntu-latest
  #   needs: build_and_push
  #   steps:
  #     - name: Deploy to VPS via SSH
  #       uses: appleboy/ssh-action@v1.0.3 # A popular GitHub Action for SSH
  #       with:
  #         host: ${{ secrets.VPS_HOST }}
  #         username: ${{ secrets.VPS_USERNAME }}
  #         key: ${{ secrets.VPS_SSH_KEY }} # Your private SSH key
  #         script: |
  #           # Navigate to your project directory on the VPS
  #           cd /path/to/your/project
            
  #           # Pull the latest Docker images (this will pull the 'latest' tag)
  #           # If you used a specific tag (e.g., commit SHA), you might need to update your docker-compose.yml
  #           # or dynamically inject the tag here. For 'latest', this is simpler.

  #           docker compose pull auth
            
  #           # Restart the services to use the new images
  #           docker-compose up -d --no-deps --build auth
            
  #           # Optional: Clean up old Docker images to save space
  #           docker image prune -f
