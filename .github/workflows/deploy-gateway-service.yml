name: deploy-gateway-service

on:
  push:
    branches:
      - main
    paths:
      - "gateway-service/**"
  workflow_dispatch:
    inputs:
      reason:
        description: "Reason for manual gateway deployment"
        required: false
        default: "Manual re-deployment for missed changes"

# Removed top-level concurrency

jobs:
  build_and_push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./gateway-service
          file: ./gateway-service/Dockerfile
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/kaj-bazar-gateway:latest

  deploy_to_vps:
    runs-on: ubuntu-latest
    needs: build_and_push

    # 🚨 CRITICAL: Shared lock
    concurrency:
      group: kaj-bazar-deployment-lock
      cancel-in-progress: true

    steps:
      - name: Pull and restart docker image
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT }}
          script: |
            echo "Deployment locked: Gateway Service deploying..."
            cd docker-host/
            docker compose pull gateway
            docker compose up gateway -d
            docker image prune -f
            echo "Gateway Service deployment complete."
